"use client";

// import historyRegistration from

function UserHistoryPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-[var(--primary-1)] via-[var(--primary-4)] to-[var(--primary-2)] flex flex-col items-center justify-center p-6">
      History
    </div>
  );
}

export default UserHistoryPage;

import React from "react";

interface Props {}

//this will be a user avatar in a circle with a drop down menu on hovering over it containing history , profile , registered hackathons

function UserProfileAvatar(props: Props) {
  const {} = props;

  return <div></div>;
}

export default UserProfileAvatar;

//this will open up when team leader clicked on add member , in the middle pf the screen 50/50 w/h 
//will ask email of the user to be added
// first will check wheather email is there in db or not if not will notify user there only
    //V2:will directly send a mail to user asking to signup to our app 
//if it is there will create a team-request with teamid and user-id found using email
import React from 'react'

interface Props {}

function SearchParticipantModal(props: Props) {
    const {} = props

    return (
        <div>

        </div>
    )
}

export default SearchParticipantModal
